- step:
      name: train
      image: tensorflow/tensorflow:1.13.1-gpu-py3
      command:
          - pip install Cython
          - apt-get update
          - apt-get install wget
          - pip install --upgrade pip
          - apt-get install -y libsm6 libxext6 libxrender-dev
          - apt install libfontconfig1 libxrender1
          - pip install opencv-python
          - pip install matplotlib
          - pip install numpy
          - apt-get install tar
          - mkdir c_annotations 
          - tar -zxvf c_annotations.tar.gz -C c_annotations/ && rm -r c_annotations.tar.gz
          - bash train_configs.sh
          - cp -r ckpt/ /valohai/outputs # save weights
          
- step:
      name: test_predict
      image: tensorflow/tensorflow:1.13.1-gpu-py3
      inputs: datum://017002ca-40df-f4eb-190b-d9a69b461cd0 
      command: cd /valohai/inputs/ && pwd && ls
